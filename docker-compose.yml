version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: book
    environment:
      - DATABASE_URL=${DATABASE_URL}
    ports:
      - "${BOOKSERVICE_LOCAL_PORT}:${BOOKSERVICE_DOCKER_PORT}"